# Implementation plan — 012 Per-resource consumers: the deeper numbers

[docs](../../../index.md) / [Roadmap](../../index.md) / [012 — Per-resource consumers: the deeper numbers](index.md) / **Implementation plan**

Empty on purpose. The first-pass per-resource sections shipped with the
dashboard redesign (see `index.md`'s "Shipped with the redesign"
table). The three deeper numbers still owed — real per-process disk I/O
rate, per-process network bandwidth, a real per-process energy figure —
each need a new platform-specific data source and go through a design
doc plus a `review-panel` pass before tasks land here.
