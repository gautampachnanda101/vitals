{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'test \"$VITALS_PREFLIGHT\" = 1 || exit 0; command -v vitals >/dev/null 2>&1 || exit 0; out=$(vitals --no-color doctor 2>/dev/null); test $? -eq 0 && exit 0; printf \"vitals pre-flight — this machine is not healthy right now:\\n\\n%s\\n\\nRun /vitals-doctor for the full breakdown.\\n\" \"$out\"'"
          }
        ]
      }
    ]
  }
}
